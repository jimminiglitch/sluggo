{
  "name": "sluggo-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./sluggo run dev",
    "build": "npm --prefix ./sluggo run build",
    "preview": "npm --prefix ./sluggo run preview",
    "install:app": "npm --prefix ./sluggo install"
  }
}
